name: Test

on: [push]

jobs:
  test:
    container:
      image: node:10.16-jessie


# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#     - uses: actions/checkout@v1
#     - runs:
#         using: 'docker'
#         image: './docker/builder/Dockerfile'
#     # - run: cargo test --all
